json.extract! user, :id, :email, :username, :first_name, :last_name, :created_at
# json.profile_img asset_path(user.avatar.url(:original))
# json.profile_cover_photo asset_path(user.cover_photo.url(:original))